> 04_functions_programming/LESSON.md を使って、if, for,
   function の基礎を学びたいです。
   私が自分でコードを書く時間を取れるように、説明は短め、問題
   は多めにしてください。
   次の R
   コードでエラーが出ました。原因を特定し、どこをどう直すかだ
   けをまず教えてください。完成コードはまだ出さないでください
   。
   [ここにコード]
   [ここにエラー文]
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 承知いたしました！説明は最小限に、問題をどんどん出していきま
  す。
  （※エラーが出たというコードとエラー文が空欄だったので、もしお
  手元にあれば貼り付けてください。まずは 04
  の基礎から進めます！）

  ---

  1. 自作関数 (function)
  R では 名前 <- function(入力) { 処理 } で道具を作ります。

  【例】

   1 double <- function(x) {
   2   x * 2
   3 }

  【問題 1】
  x を受け取り、その「3乗」を返す関数 triple_power
  を作ってください。作ったあと、triple_power(3)
  を実行して結果（27になるはず）を教えてください。

  ---

  2. 条件分岐 (if)
  if (条件) { A } else { B } で処理を分けます。

  【例】

   1 if (x > 10) { "Big" } else { "Small" }

  【問題 2】
  変数 score が 80 以上なら "Pass"、それ以外なら "Fail"
  と表示する if 文を書いてください。
  （まず score <- 85
  と代入してから実行し、結果を教えてください）

  ---

  3. 繰り返し (for)
  for (変数 in 範囲) { 処理 } で繰り返します。

  【例】

   1 for (i in 1:3) { print(i) }

  【問題 3】
  for ループを使って、1 から 5
  までの数字をそれぞれ「2乗」した値を順番に表示（print）してく
  ださい。
  （表示結果：1, 4, 9, 16, 25 が順番に出れば正解です）

  ---

  まずはこの3問、コードを書いて実行してみてください！
  もし途中でエラーが出たら、そのコードとエラー文をそのまま貼っ
  てください。原因を特定するヒントを出します。
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > triple_power(3) でエラー:
     関数 "triple_power" を見つけることができませ
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 典型的なエラーですね！原因と直し方のヒントを伝えます。

  原因
  R が 「triple_power という名前の道具（関数）をまだ知らない」
  という状態です。
  コードを書いただけではダメで、一度 R
  にその定義を「読み込ませる（実行する）」必要があります。

  直し方のヒント
   1. 定義を先に実行する:
      triple_power <- function(x) { ... } という 3
  行くらいのコードを、まずコンソールに貼り付けて Enter
  を押してください。
   2. スペルを確認する:
      定義したときの名前（左側）と、使うときの名前が
  1文字も違わず同じ
  か確認してください（大文字・小文字も区別されます）。

  まずは、triple_power <- ...
  から始まる定義の部分をしっかり実行してから、もう一度
  triple_power(3) を試してみてください！
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > 27,"Pass",[1] 1
   [1] 4
   [1] 9
   [1] 16
   [1] 25
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 全問正解です！triple_power の定義も無事に R
  に伝わりましたね。
  次は、これらの要素を組み合わせた「少しだけ複雑な問題」に挑戦
  しましょう。

  ---

  1. 関数 + 条件分岐（if）
  「もし〜なら」を関数の中に閉じ込めます。

  【問題 4：偶奇判定】
  整数を受け取り、偶数なら "even"、奇数なら "odd" を返す関数
  check_parity を作ってください。
  （ヒント：x %% 2 == 0 が「x を 2 で割った余りが
  0」という意味です）

  ---

  2. 関数 + 繰り返し（for）
  「何度もやる」を関数の中に閉じ込めます。

  【問題 5：1 から n までの和】
  数字 n を受け取り、1 から n までの合計を計算して返す関数
  sum_upto を作ってください。
  （ヒント：最初に total <- 0 という変数を作り、for (i in 1:n)
  の中で total <- total + i と足し続けてみてください）

  ---

  3. 論理演算子（&, |）
  「かつ (&)」「または (|)」を使って条件を詳しくします。

  【問題 6：特定の範囲】
  変数 x が 10 以上 かつ 20 以下 なら "In"、そうでなければ
  "Out" と表示する if 文を書いてみてください。
  （まず x <- 15 と代入してから実行し、次に x <- 25
  で試してみてください）

  ---

  コードを書いてみて、また結果（あるいはエラー）を教えてくださ
  い！
  もし「関数の中で合計を出す書き方」が難しければ、すぐにヒント
  を出します。
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > [1] "even"
   [1] "odd",[1] 55
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 正解です！55（1から10までの和）と偶奇判定、完璧に動作していま
  す。
  論理演算子 &（かつ）を使った 問題 6 はどうなりましたか？

  もし問題 6 が終わっていたら、次は if, for, function
  すべてを組み合わせた総仕上げ問題 です。

  ---

  【総仕上げ：最大値を探す関数】
  R には max()
  という便利な関数がありますが、あえてこれを使わずに、自分の力
  で最大値を探すプログラム を書いてみましょう。

  【問題 7：My Max】
  数値のベクトル v を受け取り、その中の 最大値 を探して返す関数
  my_max を作ってください。

  【ヒント（アルゴリズム）】
   1. まず、暫定のチャンピオン（最大値）を current_max <- v[1]
      （1番目の要素）にします。
   2. for ループで、2番目から最後まで順番に見ていきます。
   3. もし、今見ている数字が current_max
      より大きかったら、その数字を新しいチャンピオン（current_m
      ax）に書き換えます。
   4. 最後に current_max を返します。

  ---

  【デバッグ・トレーニング】
  あなたが先ほど仰っていた「エラーが出たコード」はありますか？
  もしあれば、そのコードとエラー文を貼り付けてください。一緒に
  原因を特定しましょう。

  特になければ、上記の 問題 7 に挑戦してみてください！
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > # x <- 15 の結果
   [1] "In"

   # x <- 25 の結果
   [1] "Out"
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 完璧です！論理演算子
  &（かつ）もバッチリ使いこなせていますね。

  ---

  それでは、「04_functions_programming（関数・条件分岐・繰り返
  し）」 の最終問題 「問題 7：My Max」 に挑戦してみましょう。
  これができれば、プログラミングの基礎的な思考回路が整ったと言
  えます！

  【問題 7：自作の最大値関数】
  数値のベクトル v（例：c(3, 8, 2, 10, 5)）を受け取り、その中の
  最大値（この場合は 10）を返す関数 my_max を作ってください。

  【構成のヒント（穴埋め）】

    1 my_max <- function(v) {
    2   # 1. 最初の値を暫定の最大値にする
    3   res <- v[1]
    4
    5   # 2. 2番目の要素から最後までループする
    6   for (i in 2:length(v)) {
    7     # 3. もし v[i] が今の最大値(res)より大きかったら...
    8     if (v[i] > res) {
    9       # 4. 最大値を更新する！
   10       res <- v[i]
   11     }
   12   }
   13
   14   # 5. 最後に最大値を返す
   15   return(res)
   16 }

  このコードを打ち込んで、実際に my_max(c(3, 8, 2, 10, 5))
  を試してみてください。
  結果はどうなりましたか？

  ---

  ここまでの内容はすべて理解できましたか？
  もし大丈夫そうなら、次は
  「05_numerical_computing（数値計算）」 に進みましょう。
  ここでは、R
  が得意とする統計的な数値の扱いや、行列計算について学びます。
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > [1] 99
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 素晴らしい！99 と正しく最大値が返ってきましたね。
  v[1] から始めて、ループの中で if
  文を使って「今の最大値より大きいか？」をチェックし、更新して
  いくという アルゴリズム（計算の手順）
  を自分の手で実装できました。これは大きな一歩です！

  これで 「04_functions_programming（関数・制御構造）」
  は完璧です。

  ---
