▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > 03_vectors_matrices_lists/LESSON.md
   を使って、ベクトル→行列→リストの順で教えてください。
   毎回「これは何次元か」「どうアクセスするか」を確認する問題
   を出してください。
   apply() ファミリーが分かりません。
   03_vectors_matrices_lists/LESSON.md を見て、apply, lapply,
   sapply
   の違いを、最小限の例で説明し、その後に練習問題を2問出してく
   ださい。
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 教材の 03_vectors_matrices_lists/LESSON.md と EXERCISES.md
  を確認し、一歩ずつ進めていきましょう！

  まずは「ベクトル、行列、リスト」の3つのデータ構造を順番にマス
  ターします。

  ---

  1. ベクトル (Vector)
  R
  の基本中の基本です。同じ種類のデータ（数値だけ、文字だけなど
  ）を横一列に並べたものです。

  【作り方】
  c() という関数を使って作ります。c は
  "combine"（つなげる）の略です。

   1 v <- c(10, 20, 30, 40, 50)

  【次元とアクセス】
   - 次元: 1次元（長さだけがある）
   - アクセス: v[2] のように、[ ]
     と番号（インデックス）を使います。

  【ミニテスト】
   1. v <- c(10, 20, 30, 40, 50) の 次元 は何次元ですか？
   2. v
      の「3番目の要素（30）」を取り出すには、どう書けばよいでし
      ょうか？

  ---

  2. 行列 (Matrix)
  ベクトルを縦横に並べた「表」のようなデータ構造です。すべて同
  じ種類のデータである必要があります。

  【作り方】
  matrix() 関数を使います。

   1 m <- matrix(1:6, nrow = 2, ncol = 3)
   2 # 1から6までの数字を、2行3列に並べる

  【次元とアクセス】
   - 次元: 2次元（行と列がある）
   - アクセス: m[行番号, 列番号] のように書きます。

  【ミニテスト】
   1. m の 次元 は何次元ですか？
   2. m
      の「1行目の2列目」を取り出すには、どう書けばよいでしょう
      か？

  ---

  3. リスト (List)
  「何でも入れられる箱」です。数値、文字、ベクトル、行列、さら
  には別のリストまで、バラバラな種類のデータを一つにまとめられ
  ます。

  【作り方】
  list() 関数を使います。

   1 L <- list(name = "Taro", score = c(80, 90, 70), data = m)

  【次元とアクセス】
   - 次元: 1次元ですが、中身は複雑です。
   - アクセス:
       - L[[2]] のように、[[
         ]]（二重カッコ）を使って中身を取り出す。
       - L$score のように、$ と名前を使って取り出す。

  【ミニテスト】
   1. L のようなリストの 次元
      は、基本的には何次元として扱われますか？（1次元か、2次元
      か）
   2. L の中の score（80, 90, 70）を取り出すには、$
      を使ってどう書けばよいでしょうか？

  まずは、ここまでの「次元」と「アクセス方法」の答えを教えてく
  ださい！そのあと、apply ファミリーの解説に入ります。

  ✓  ReadFile  03_vectors_matrices_lists\LESSON.md
  ✓  ReadFile  03_vectors_matr...\...\EXERCISES.md

✦ はい、ベクトル、行列、リストの基本概念を整理しました。
  それぞれの「次元」と「取り出し方」について、まずはこちらのミ
  ニテストに答えてみてください！

  ---

  【ミニテスト】ベクトル・行列・リスト

  1. ベクトル
  v <- c(10, 20, 30, 40, 50)
   - 次元: 何次元ですか？
   - アクセス: v
     の「3番目の要素（30）」を取り出すには、どう書けばよいでし
     ょうか？

  2. 行列
  m <- matrix(1:6, nrow = 2, ncol = 3)
   - 次元: 何次元ですか？
   - アクセス: m
     の「1行目の2列目」を取り出すには、どう書けばよいでしょうか
     ？

  3. リスト
  L <- list(name = "Taro", score = c(80, 90, 70))
   - 次元:
     基本的には何次元として扱われますか？（1次元か、2次元か）
   - アクセス: L の中の score（80, 90, 70）を取り出すには、$
     を使ってどう書けばよいでしょうか？

  ---

  この答えが確認でき次第、一番の難所である apply
  ファミリー（apply, lapply, sapply） の解説に移ります！
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > 1次元、v[３]、２次元、m[1.2]
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 惜しいです！非常に良い線を行っていますが、2つだけ大切な修正ポ
  イントがあります。

  【修正アドバイス】

   1. 記号の使い分け（カンマとドット）
       * 行列のアクセスは m[1, 2] のように カンマ ,
         を使います。
       * m[1.2] と書くと、R
         は「1.2番目（小数）」と解釈してしまい、正しく取り出せ
         ません。

   2. 全角と半角
       * R コンソールで実行するときは、数字や記号（[ ] ,
         3）はすべて 半角 で入力する必要があります。v[３]
         のように全角の ３
         を使うとエラーになるので注意してください。

  ---

  【残りの確認：リスト】
  リスト L <- list(name = "Taro", score = c(80, 90, 70))
  についても答えてみてください！

   1. 次元:
      リストは「1次元（一列に並んだ箱）」ですか？それとも「2次
      元（縦横の表）」ですか？
   2. アクセス: L の中の score を取り出すには、$
      を使ってどう書けばよいでしょうか？（ヒント：L$名前
      の形です）

  これに答えていただければ、いよいよ apply ファミリー
  の解説に入ります！
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > 1次元、L$score
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 正解です！リストは「1次元（並んだ箱）」、アクセスは L$score
  ですね。

  それでは、R の中級者への第一歩である apply ファミリー
  の解説に入ります。
  これらは、一言で言うと
  「バラバラのデータに、同じ処理をまとめて一気にかける魔法」
  です。

  ---

  4. apply ファミリー：一気に計算する仕組み

  ① apply()：行列（Matrix）の「行」や「列」ごとに処理する
  行列の「すべての行の合計」や「すべての列の平均」を一発で出し
  ます。

   * 書き方: apply(行列, 方向, 関数)
   * 方向: 1 は「行（横方向）」、2 は「列（縦方向）」

   1 m <- matrix(1:6, nrow = 2, ncol = 3)
   2 # 1 3 5
   3 # 2 4 6
   4
   5 # すべての列(2)の合計を一気に出す
   6 apply(m, 2, sum)
   7 # 結果: 3 7 11  (1+2, 3+4, 5+6)

  ②
  lapply()：リスト（List）の各要素に処理し、結果を「リスト」で
  返す
  l は List の略です。

   1 L <- list(a = c(1, 2, 3), b = c(10, 20))
   2
   3 # 各要素の合計を一気に出す
   4 lapply(L, sum)
   5 # 結果: リスト形式で $a が 6, $b が 30

  ③
  sapply()：リストに処理し、結果を「ベクトル」でスッキリ（Simpl
  e）返す
  s は Simple
  の略です。中身が単純な数値なら、リストではなくベクトルにして
  くれます。

   1 # 同じ計算を sapply でやる
   2 sapply(L, sum)
   3 # 結果: a  b
   4 #       6 30  (スッキリしたベクトル形式！)

  ---

  【練習問題：apply ファミリー】

  以下の 2 問に挑戦してみましょう。

  問 1：行列の「行」ごとの平均
  以下の行列 mat を作り、apply() を使って
  「行（横方向）ごとの平均 (mean)」 を求めてください。

   1 mat <- matrix(1:9, nrow = 3)
   2 # 1 4 7
   3 # 2 5 8
   4 # 3 6 9
  ヒント: apply(mat, 方向, mean) の「方向」を 1 か 2
  どちらにするか考えてみてください。

  問 2：リストの「長さ」をスッキリ出す
  以下のリスト mylist の、「それぞれの要素の長さ (length)」 を
  sapply() を使って求めてください。

   1 mylist <- list(x = c(1, 2, 3), y = "Hello", z = c(TRUE,
     FALSE))
  ヒント: sapply(mylist, length)
  と打つだけです。結果がどう表示されるか確認してみてください。

  実行結果を教えてください！
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > [1] 4 5 6,
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ 正解です！apply(mat, 1, mean)
  を実行して、行（横方向）ごとの平均 4, 5, 6
  が正しく求められましたね。

   - 1行目: (1+4+7)/3 = 4
   - 2行目: (2+5+8)/3 = 5
   - 3行目: (3+6+9)/3 = 6

  ---

  【問 2 の確認】
  もう一つの sapply() を使った問題はどうなりましたか？

   1 mylist <- list(x = c(1, 2, 3), y = "Hello", z = c(TRUE,
     FALSE))
   2 sapply(mylist, length)

  これを実行すると、リストの各要素の「長さ」がスッキリした形で
  表示されるはずです。

   1 x y z
   2 3 1 2
  （x は3つ、y は1つ、z
  は2つのデータが入っていることが分かります）

  これで
  「03_vectors_matrices_lists（ベクトル・行列・リスト）」
  もマスターしました！

  ---

  次は 「04_functions_programming（関数と制御構造）」
  に進みましょう。
  ここでは、「自分で関数を作る方法」や、「もし〜なら（if）」「
  〜の間繰り返す（for）」といった、プログラムらしい動きを学びま
  す。

  まずは 04_functions_programming/LESSON.md
  の内容を確認しますね。

  ✓  ReadFile  04_functions_programming\LESSON.md
  ✓  ReadFile  04_functions_pr...\...\EXERCISES.md

✦ それでは
  「04_functions_programming（関数・条件分岐・繰り返し）」
  のレッスンを開始します。
  一気に「プログラミングらしく」なりますが、一つずつ丁寧に紐解
  いていきましょう！

  ---

  1. 関数の作り方 (function())

  R
  では、自分専用の「便利な道具（関数）」を作ることができます。
  箱に何かを入れる（入力）と、何かが返ってくる（出力）というイ
  メージです。

  【予想】
  以下のコードを実行して「関数 square」を作ったあと、square(5)
  と打つと何が返ってくるでしょうか？

   1 square <- function(x) {
   2   x^2
   3 }

  【実行】
   1. まず上の 3 行をコピーして R に貼り付け、Enter
      を押してください。
   2. 次に square(5) と打って、結果を教えてください。

  【ヒント】
  x という名前の「入り口」から入った数字（この場合は
  5）が、x^2（5の2乗）となって返ってきます。

  ---

  2. 条件分岐 (if / else)

  「もし〜なら、これをする。そうでなければ、あれをする」という
  仕組みです。

  【予想】
  以下のコードを実行したとき、画面にはどちらの文字が表示される
  でしょうか？

   1 x <- -10
   2
   3 if (x > 0) {
   4   print("Positive")
   5 } else {
   6   print("Non-positive")
   7 }

  【実行】
  実際に上のコードを順番に実行して、結果を貼り付けてください。

  【ヒント】
  x は -10 です。x > 0（-10 は 0
  より大きいか？）という条件が正しいか間違っているか、考えてみ
  てください。

  ---

  3. 繰り返し (for ループ)

  「同じことを何度も繰り返す」ときに使います。

  【予想】
  以下のコードを実行すると、画面には何が表示されるでしょうか？

   1 for (i in 1:3) {
   2   print(i * 10)
   3 }

  【実行】
  実際に実行して、結果を貼り付けてください。

  【ヒント】
  i という変数が、最初は 1、次は 2、最後は 3
  と順番に変わっていき、そのたびに print(i * 10)
  が実行されます。

  ---

  まずは、この3つの実行結果を教えてください！そのあと、練習問題
  に進みます。
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > x y z
   3 1 2
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
✦ sapply の結果、バッチリですね！x は要素3つ、y は1つ、z
  は2つと、リストの中身がスッキリ一覧できました。

  では、先ほどお出しした
  「04_functions_programming（関数・条件分岐・繰り返し）」
  の3つのミニテストに挑戦してみましょう！

  ---
